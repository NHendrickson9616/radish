# Core defaults embedded in the Radish binary.
# A user config can contain only the values it wants to override.
version: 1

# Settings understood by the base application.
database: radish.db
library: ~/Music
dry_run: false
import:
  max_depth: 10
  follow_symlinks: false
  analysis: basic
